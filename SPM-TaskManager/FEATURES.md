New features added:

- `task_stats.py`: computes basic statistics from a JSON tasks file (counts by status, total tasks). Usage: `python task_stats.py tasks.json`.
- `export_tasks.py`: exports tasks from JSON to CSV. Usage: `python export_tasks.py tasks.json out.csv`.

These are lightweight utilities intended to assist with simple task list workflows and demonstrations.
